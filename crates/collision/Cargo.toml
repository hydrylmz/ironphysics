[package]
name    = "physics_collision"
version = "0.1.0"
edition = "2021"

[dependencies]
physics_math = { path = "../math" }
physics_core = { path = "../core" }
smallvec     = { version = "1.13", features = ["union"] }

[dev-dependencies]
approx = "0.5"